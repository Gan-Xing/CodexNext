import { DesignLab } from "../../../components/DesignLab";

export const metadata = {
  title: "Settings Flow | CodexNext Design Lab",
  description: "Account, device, and preference design placeholders."
};

export default function SettingsDesignPage() {
  return <DesignLab flow="settings" initialState="empty" />;
}
